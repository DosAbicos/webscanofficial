# 🚀 Развертывание на Railway.app

## 📋 Подготовка (5 минут)

### Шаг 1: Создайте GitHub репозиторий

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например: `barcode-inventory-manager`)
4. Сделайте его **Private** (для безопасности)
5. Нажмите "Create repository"

### Шаг 2: Загрузите код в GitHub

**На вашем компьютере (в терминале Emergent или локально):**

```bash
cd /app

# Инициализируйте git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit"

# Добавьте remote (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Отправьте код
git push -u origin main
```

**Если попросит аутентификацию:**
- Username: ваш GitHub username
- Password: используйте **Personal Access Token** (не обычный пароль)
  - Создайте токен: Settings → Developer settings → Personal access tokens → Generate new token

---

## 🚂 Развертывание на Railway (10 минут)

### Шаг 3: Создайте аккаунт на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Войдите через GitHub (кнопка "Login with GitHub")
4. Разрешите Railway доступ к вашим репозиториям

### Шаг 4: Создайте новый проект

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Найдите и выберите ваш репозиторий `barcode-inventory-manager`
4. Railway автоматически начнёт деплой

### Шаг 5: Настройте переменные окружения

**Для Backend:**

1. В проекте Railway нажмите на ваш сервис
2. Перейдите на вкладку "Variables"
3. Добавьте переменные:

```
MONGO_URL=mongodb://localhost:27017
DB_NAME=barcode_manager
CORS_ORIGINS=*
PORT=8001
```

4. Нажмите "Add" или "Save"

### Шаг 6: Добавьте MongoDB (если нужна база данных)

**Внимание:** Ваше приложение работает БЕЗ базы данных (использует IndexedDB в браузере). MongoDB используется только для примера в коде. Можно **пропустить** этот шаг, или:

1. В проекте нажмите "+ New"
2. Выберите "Database" → "Add MongoDB"
3. Railway создаст базу и автоматически добавит переменную `MONGO_URL`

### Шаг 7: Получите URL вашего приложения

1. В вашем сервисе перейдите в "Settings"
2. Найдите секцию "Domains"
3. Нажмите "Generate Domain"
4. Скопируйте URL (например: `barcode-manager-production.up.railway.app`)

### Шаг 8: Обновите Frontend для работы с Railway

**Важно!** Нужно обновить URL backend в frontend:

1. Откройте файл `/app/frontend/.env`
2. Измените `REACT_APP_BACKEND_URL`:

```env
REACT_APP_BACKEND_URL=https://your-app-name.up.railway.app
```

3. Замените `your-app-name.up.railway.app` на ваш реальный URL из Шага 7

4. Сохраните и запушьте изменения:

```bash
cd /app
git add frontend/.env
git commit -m "Update backend URL for Railway"
git push
```

5. Railway автоматически пересоберёт приложение

---

## ✅ Проверка

### Шаг 9: Проверьте работу

1. Откройте ваш Railway URL в браузере
2. Должна загрузиться ваша таблица с товарами
3. Попробуйте:
   - Поиск товара ✅
   - Сканирование штрихкода (нажмите кнопку сканера) ✅
   - Экспорт Excel (кнопка "Экспорт" справа вверху) ✅

### Шаг 10: Проверьте экспорт

1. Нажмите "Экспорт"
2. Скачайте файл `.xls`
3. Откройте в Excel
4. Проверьте:
   - Файл открывается ✅
   - Форматирование сохранено (цвета, размеры) ✅
   - Штрихкоды на месте ✅

---

## 🔧 Дополнительные настройки (опционально)

### Добавьте свой домен

1. В Railway → Settings → Domains
2. Нажмите "Custom Domain"
3. Введите ваш домен (например: `inventory.yourdomain.com`)
4. Добавьте CNAME запись в настройках вашего домена:
   - Name: `inventory` (или что хотите)
   - Value: `your-app.up.railway.app`
5. Подождите 5-10 минут для активации DNS

### Мониторинг

Railway автоматически показывает:
- Логи приложения (вкладка "Deployments" → "View Logs")
- Использование ресурсов (CPU, RAM)
- Статус деплоя

---

## 💰 Стоимость

**Railway Pricing:**
- Бесплатно: $5 кредитов (хватит на ~500 часов работы)
- Платный план: $5/месяц за активное использование

**Ваше приложение:**
- Backend работает постоянно: ~$5/месяц
- Frontend статический: бесплатно (отдаётся через CDN)

**Оптимизация расходов:**
- Railway считает только время когда сервер активен
- Ваше приложение легковесное → будет стоить около $5/месяц

---

## 🐛 Решение проблем

### Проблема: "Build failed"

**Решение:**
1. Проверьте логи: Deployments → View Logs
2. Убедитесь что все файлы закоммичены в git
3. Проверьте `requirements.txt` и `package.json`

### Проблема: "Application Error" при открытии

**Решение:**
1. Проверьте переменные окружения (Шаг 5)
2. Убедитесь что `PORT` не задан или равен 8001
3. Проверьте логи приложения

### Проблема: Экспорт не работает

**Решение:**
1. Убедитесь что `REACT_APP_BACKEND_URL` правильно настроен
2. Проверьте что `CORS_ORIGINS=*` в переменных backend
3. Откройте DevTools (F12) → Console - посмотрите ошибки

### Проблема: MongoDB ошибка

**Решение:**
Ваше приложение НЕ использует MongoDB для основной работы! Можете:
1. Удалить MongoDB сервис из Railway
2. Или оставить переменную `MONGO_URL=mongodb://localhost:27017` (не будет использоваться)

---

## 📱 Использование

После успешного деплоя:

1. **Откройте на телефоне**: ваш Railway URL
2. **Добавьте на домашний экран** (работает как приложение!)
   - Chrome Android: Меню → "Добавить на главный экран"
   - Safari iOS: Поделиться → "На экран Домой"
3. **Работайте офлайн**: всё работает без интернета!
4. **Экспортируйте**: подключитесь к интернету для экспорта с форматированием

---

## 🎉 Готово!

Ваше приложение теперь доступно 24/7 через интернет!

**Следующие шаги:**
- Добавьте в закладки на телефоне
- Протестируйте все функции
- Поделитесь ссылкой с коллегами (если нужно)

**Нужна помощь?** Напишите мне, я помогу разобраться! 🚀

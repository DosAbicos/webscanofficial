# 🔧 Настройка переменных окружения для Railway

## Backend переменные (обязательно)

В Railway → ваш проект → Variables, добавьте:

```env
MONGO_URL=mongodb://localhost:27017
DB_NAME=barcode_manager
CORS_ORIGINS=*
```

**Примечание:** MongoDB не обязателен для работы приложения. Основные данные хранятся в IndexedDB браузера.

---

## Frontend переменные (после получения Railway URL)

### Шаг 1: Получите ваш Railway URL

1. Railway → Settings → Domains → Generate Domain
2. Скопируйте URL (например: `my-app.up.railway.app`)

### Шаг 2: Обновите frontend/.env

В файле `/app/frontend/.env` измените:

```env
REACT_APP_BACKEND_URL=https://ваш-реальный-url.up.railway.app
```

⚠️ **Важно:** Замените `ваш-реальный-url` на ваш настоящий Railway URL!

### Шаг 3: Запушьте изменения

```bash
cd /app
git add frontend/.env
git commit -m "Update backend URL for production"
git push
```

Railway автоматически пересоберёт приложение.

---

## Проверка

После деплоя откройте DevTools (F12) → Console:

✅ **Правильно:** `🌐 Attempting backend export...`
❌ **Неправильно:** `Failed to fetch` или `CORS error`

Если видите ошибку:
1. Проверьте что `REACT_APP_BACKEND_URL` правильный
2. Убедитесь что `CORS_ORIGINS=*` в backend переменных
3. Перезапустите деплой: Railway → Deployments → Redeploy

---

## Для локальной разработки

Если хотите продолжить разработку локально:

**frontend/.env.local** (создайте новый файл):
```env
REACT_APP_BACKEND_URL=http://localhost:8001
```

Это позволит локально работать с локальным backend, а в продакшене использовать Railway URL.
